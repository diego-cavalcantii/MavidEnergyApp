<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Fundo Branco -->
    <solid android:color="@color/white" />

    <!-- Borda -->
    <stroke
        android:width="4dp"
        android:color="@color/icon_button_selected" />

    <!-- Cantos Arredondados -->
    <corners android:radius="6dp" />
</shape>
